{
  "name": "photos-saver-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm dlx wrangler dev",
    "build": "pnpm dlx wrangler deploy --dry-run",
    "deploy": "pnpm dlx wrangler deploy",
    "deploy:version": "pnpm dlx wrangler versions upload",
    "typecheck": "tsc --noEmit",
    "test": "vitest run tests"
  },
  "dependencies": {
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
